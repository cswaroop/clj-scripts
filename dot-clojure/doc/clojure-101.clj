;; What are the most important features of a programming language?
;; 1. Primitive data types
;; 2. means of combinatino that provides
;; 3. means of abstraction

;; Primitives

;; Booleans, Characters, Keywords, Symbols, Lists
;; Numbers, Sets, Maps, Vector, Nil

;; Symbols
;; #'foo #'+
[
 ;; ## Creating abstractions
 ;; `def` and `defn` are the means of abstraction
 ;; def - binds a name to a value
 ;; defn - binds a name to a function
 ;;
 (+ 1 2)]


; in the

(def abcdfd {:a 1})
             ;;:b 2})
(+ 1 2)
