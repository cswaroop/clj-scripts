(ns functional-programmeing)

;; Thinking from
;; - map, reduce, filter
;; - apply, comp, partial, complement

;;
;; do - realize for size effects
;; doall, dorun - Lazy seq
;; doseq, dotimes - repeated exectuion
;; doto - Java Interop
;; dosync - transaction for refs


;; *1 *2 *3 *e
;; *in* *err*
;; *ns* *file*
